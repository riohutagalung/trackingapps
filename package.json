{
  "name": "rh-habits",
  "version": "2.4.1",
  "private": true,
  "scripts": {
    "cap:add:android": "npx cap add android",
    "cap:add:ios": "npx cap add ios",
    "native:prepare": "node scripts/setup-native.mjs",
    "cap:sync": "npm run native:prepare && npx cap sync",
    "cap:copy": "npm run native:prepare && npx cap copy",
    "cap:open:android": "npx cap open android",
    "cap:open:ios": "npx cap open ios",
    "postinstall": "node scripts/patch-capgo-ios.mjs",
    "ios:prepare": "npm run native:prepare && npx cap sync ios",
    "ios:doctor": "node scripts/verify-ios.mjs",
    "ios:open": "npx cap open ios",
    "ios:build:sim": "xcodebuild -project ios/App/App.xcodeproj -scheme App -sdk iphonesimulator -configuration Debug -derivedDataPath ios/build CODE_SIGNING_ALLOWED=NO"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/camera": "^8.2.4",
    "@capacitor/core": "^8.5.1",
    "@capacitor/ios": "^8.5.1",
    "@capacitor/splash-screen": "^8.0.2",
    "@capgo/background-geolocation": "8.4.5"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.3",
    "typescript": "^5.9.3"
  }
}
